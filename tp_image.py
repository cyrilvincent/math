# Obtenir les canaux RED, GREEN, BLUE
# Afficher la dynamique, la luminance, le contraste des canaux : min, max, avg, std
# Afficher la transposée
# Modifier la luminance de n dans une fonction : np.clip(x,0,255)
# Cropper l'image de n, par exemple pour n=10 (10,10,10,10)
# Créer un vrai image N&B : avg de la 3ème dimension
# Superposer 2 images /!\ les images n'ont pas la même résolution
# Calculer le contraste, autoconstraster l'image